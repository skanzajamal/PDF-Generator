CREATE TABLE staff(
  id            INTEGER       NOT NULL,
  full_name     VARCHAR(64)   NOT NULL,
  job_title     VARCHAR(64)   NOT NULL,
  department    VARCHAR(64)   NOT NULL,
  PRIMARY KEY (id)
);